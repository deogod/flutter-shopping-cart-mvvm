import 'package:flutter/material.dart';

class CartInitialState extends StatelessWidget {
  const CartInitialState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Carrinho de compras vazio.'),
    );
  }
}
